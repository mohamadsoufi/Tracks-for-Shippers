import React from "react";
import FilterBy from "../FilterBy";

export default function ToolsList({ info, filterByCity }) {
    return (
        <div>
            <p>hsdfkhsdfjh</p>

            <FilterBy info={info} filterByCity={filterByCity} />
        </div>
    );
}
